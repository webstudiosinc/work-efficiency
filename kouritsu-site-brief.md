# 業務効率化情報サイト 制作ブリーフ

Claude.ai でのやり取りをもとに、実際の制作に入るための前提・方針・進め方をまとめたもの。
「決定事項」と「提案（未確定）」を分けて記載している。

---

## 1. 背景と目的

- 業務効率化に関する細かい情報を集めて、一つのサイトにまとめたい
- 当初は収益化も検討していたが、現時点では **趣味として、自分が見たい情報を自動で集めて並べるサイト** として作る方向
- 情報収集とサイト更新は、できるだけ自動化したい

### 作り手について
- 本業はLP制作（フリーランス、独立1年ほど）
- 普段の実装は WordPress、デザイン制作は HTML / CSS / JS
- Claude Code（macOS）を使用
- レンタルサーバーは Xserver を契約済み

---

## 2. 決定事項

| 項目 | 内容 |
|---|---|
| サイトの位置づけ | 趣味の情報まとめサイト |
| コスト方針 | **従量課金のAPI（Claude API 等）は使わない** |
| 技術選定 | WordPress ではなく、**Cloudflare 側の技術（Astro + Cloudflare Pages）** を試す場にする |
| 公開URL | 最初は **Cloudflare Pages の無料アドレス（〇〇.pages.dev）** で始める |
| 独自ドメイン | 1〜2か月運用して続けられそうなら取得を検討（Cloudflare Registrar 等） |

---

## 3. 提案（未確定・進めながら判断）

### 3-1. 進め方の段階
1. **Astro で一覧ページだけ作り、Cloudflare Pages に公開する**
2. **RSS 自動収集を追加する（AIなし）**
   - タイトル・フィードの抜粋・元記事リンクを一覧表示
   - カテゴリ／タグはキーワードのルールで自動付与（例：「請求書」を含む → 請求カテゴリ）
3. 1〜2週間動かして、抜粋だけでは物足りないカテゴリがあれば **要約を追加するか検討**

### 3-2. 自動化の構成案
- **RSS 収集**：Cloudflare Workers の Cron Triggers で定期実行（Mac や Xserver の cron に依存しない）
- **データ保存**：Cloudflare の D1 / KV、またはリポジトリ内のJSONをビルド時に読み込む形のどちらか
- **サイト再生成**：収集後に Cloudflare Pages のビルドを走らせる（無料プランは月500ビルドまでなので、頻度は1日1〜数回程度に）

### 3-3. 要約を入れる場合（任意・後回し）
- 従量課金APIは使わない方針のため、**Claude Code を定額プランの範囲で非対話実行（`claude -p`）** する案
- Mac の launchd / cron で1日1回実行 → 要約結果をリポジトリに書き込み → デプロイ
- 注意点
  - 定額プランの利用上限を本業と取り合うので、件数は1日数件〜十数件に抑える
  - Mac が起動していないと動かない
  - 定額プランでの自動実行の条件は変わる可能性があるため、事前に最新ドキュメントを確認する

### 3-4. ドメイン名の候補の考え方
- 長い直訳より、**短く覚えやすい名前** を優先（ドメイン名のSEO効果は小さい）
- ローマ字は表記ゆれ（gyomu / gyoumu、koritsu / kouritsu）が弱点。使うなら長音の u を省いた短い表記が迷いにくい
- 例：`koritsu`、`kouritsu-memo`、`koritsu-lab`、`worksmart`、`bizhack` など
- pages.dev 名は仮でよいが、将来同名で .com / .jp を取りたいなら空き状況だけ先に確認

---

## 4. 守るべき注意点

- **著作権**：元記事の本文を転載・長文で言い換えない。短い抜粋（または要約）＋元記事へのリンクの形にする
- **正確さ**：自動取得・自動要約の情報には「自動収集です。詳細は元記事をご確認ください」と明記する
- **将来の収益化に備える場合**：自動収集記事は「ニュース」等のカテゴリに分け、自分で書いた記事と区別できる構造にしておく
- 収益化（アフィリエイト等）する段階になったら、ステマ規制に基づく「PR」「広告」表記が必要

---

## 5. 未決定事項（Claude Code での作業開始前後に決める）

- [ ] 収集する情報源（まずは5〜10個。SaaS各社の公式ブログ・アップデート情報、行政の制度改正ページなど）とRSSの有無
- [ ] カテゴリ構成（例：会計、勤怠、請求書、AIツール など）
- [ ] pages.dev のプロジェクト名（ドメイン名候補）
- [ ] データ保存方式（D1 / KV / JSON）
- [ ] 収集・更新の頻度

---

## 6. Claude Code への最初の依頼例

```
このブリーフをもとに、業務効率化情報サイトの制作を始めたいです。
まずは「3-1 進め方の段階」の1番目として、
Astro のプロジェクトを作成し、ダミーデータの記事一覧ページを
Cloudflare Pages にデプロイできる状態にしてください。
TypeScript は慣れていないので、主要なファイルの役割も簡単に説明してください。
```
